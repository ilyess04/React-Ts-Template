enum Elang {
  fr = "fr",
  en = "en",
  ar = "ar",
}
enum ETheme {
  dark = "dark-theme",
  light = "light-theme",
}
enum EToken {
  noToken = "noToken",
  accessToken = "accessToken",
  refreshToken = "refreshToken",
}
export { Elang, ETheme, EToken };
