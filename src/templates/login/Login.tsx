import { ILoginTemplate } from "../../interfaces";

const LoginTemplate = ({
  authState,
  setAuthState,
  ...props
}: ILoginTemplate): JSX.Element => {
  return <></>;
};
export default LoginTemplate;
